const items = ['item 1', 'item 2', 'item 3', 'item 4'];

const users = [{ name: 'A' }, { name: 'B' }, { name: 'C' }, { name: 'D' }];

for (const item of items) {
	console.log(item);
}

for (const user of users) {
	console.log(user.name);
}
